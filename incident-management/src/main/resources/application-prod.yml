server:
  port: 8081
spring:
  datasource:
    url: jdbc:h2:mem:incident;DB_CLOSE_DELAY=-1;MODE=MySQL
    driver-class-name: org.h2.Driver
    username: mx
    password: admin123

  # jpa:
  #   database-platform: org.hibernate.dialect.H2Dialect
  #   hibernate:
  #     ddl-auto: update

  h2:
    console:
      enabled: true
      path: /h2-console
mybatis-plus:
  mapper-locations: classpath*:com/homework/incident/mapper/*.xml
